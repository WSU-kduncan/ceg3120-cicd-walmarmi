[Unit]
Description=Webhook listener
After=network.target

[Service]
User=ec2-user
ExecStart=/usr/local/go/bin/webhook -hooks  /home/ec2-user/hooks.json -port 9000 -verbose
Restart=always

[Install]
WantedBy=multi-user.target

